const sol = require('../../toptal');
describe('just test', () => {
    it('test', () => {
        // const a = [1, 3, 4, 6, 8]
        // expect(sol.solution(a, 4)).toBe(2);
        // expect(sol.solution(a, 3)).toBe(1);
        // expect(sol.solution(a, 1)).toBe(0);
    })
})
